<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/loginStyle.css" />
    <title>Sign in & Sign up Form</title>
    <style>
      /* Style adjustments for smaller input fields and tighter spacing */
      .input-field {
        display: flex;
        align-items: center;
        margin-bottom: 1rem; /* Reduced margin for tighter spacing */
        position: relative;
      }

      .input-field i {
        position: absolute;
        margin-left: 0.75rem; /* Adjusted icon margin */
        color: #acacac;
      }

      .input-field input,
      .input-field select {
        width: 90%; /* Reduced width to make input fields smaller */
        padding: 0.5rem 1rem 0.5rem 2.5rem; /* Adjusted padding for smaller inputs */
        border: none;
        border-radius: 2.5rem;
        background-color: #f0f0f0;
        color: #333;
        font-size: 0.9rem; /* Slightly smaller font size */
        margin-left: 0.5rem;
      }

      .input-field input:focus,
      .input-field select:focus {
        outline: none;
        background-color: #e0e0e0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="forms-container">
        <div class="signin-signup">
          <form action="#" class="sign-in-form">
            <h2 class="title">Sign in</h2>
            <div class="input-field">
              <i class="fas fa-user"></i>
              <input
                type="text"
                placeholder="Username"
                id="usernameInputLogin"
              />
            </div>
            <div class="input-field">
              <i class="fas fa-lock"></i>
              <input
                type="password"
                placeholder="Password"
                id="passwordInputLogin"
              />
            </div>
            <div class="error" id="loginError"></div>
            <input
              type="submit"
              value="Login"
              class="btn solid"
              id="submitLogin"
            />
          </form>
          <form action="#" class="sign-up-form">
            <h2 class="title">Sign up</h2>
            <div class="input-field">
              <i class="fas fa-user"></i>
              <input type="text" placeholder="Name" id="nameInputSignUp" />
            </div>
            <div class="input-field">
              <i class="fas fa-envelope"></i>
              <input
                type="username"
                placeholder="Username"
                id="usernameInputSignUp"
              />
            </div>
            <div class="input-field">
              <i class="fas fa-lock"></i>
              <input
                type="text"
                placeholder="Password"
                id="passwordInputSignUp"
              />
            </div>
            <!-- Dropdown Role -->
            <div class="input-field">
              <i class="fas fa-user-tag"></i>
              <select id="role" class="input" onchange="handleRoleChange()">
                <option value="">Select Role</option>
                <option value="panitia-lantai-1">Panitia Lantai 1</option>
                <option value="panitia-lantai-2">Panitia Lantai 2</option>
                <option value="panitia-lantai-3">Panitia Lantai 3</option>
                <option value="mudhohi">Mudhohi</option>
              </select>
            </div>
            <!-- Fields for Mudhohi -->
            <div id="mudhohi-fields" style="display: none">
              <div class="input-field">
                <i class="fas fa-map-marker-alt"></i>
                <input
                  type="text"
                  placeholder="Address"
                  id="addressInputSignUp"
                />
              </div>
              <div class="input-field">
                <i class="fas fa-phone"></i>
                <input
                  type="text"
                  placeholder="Phone Number"
                  id="phoneNumberInputSignUp"
                />
              </div>
              <div class="input-field">
                <i class="fas fa-phone"></i>
                <input
                  type="number"
                  placeholder="Nomor Sapi"
                  id="nomorSapiInputSignUp"
                />
              </div>
            </div>
            <div class="error" id="signUpError"></div>
            <input
              type="submit"
              class="btn"
              value="Sign up"
              id="submitSignUp"
            />
          </form>
        </div>
      </div>

      <div class="panels-container">
        <div class="panel left-panel">
          <div class="content">
            <h3>Belum memiliki akun?</h3>
            <p>
              Buat akun dengan role tertentu dan berpartisipasi dalam acara
              qurban Nur Shofa
            </p>
            <button class="btn transparent" id="sign-up-btn">Sign up</button>
          </div>
          <img src="/img/log.svg" class="image" alt="" />
        </div>
        <div class="panel right-panel">
          <div class="content">
            <h3>Sudah memiliki akun ?</h3>
            <p>
              Login dengan akunmu untuk masuk dan berpartisipasi dalam acara
              qurban Nur Shofa
            </p>
            <button class="btn transparent" id="sign-in-btn">Sign in</button>
          </div>
          <img src="/img/register.svg" class="image" alt="" />
        </div>
      </div>
    </div>

    <script src="/scripts/loginScript.js"></script>

    <!-- JavaScript to handle role selection -->
    <script>
      function handleRoleChange() {
        var role = document.getElementById("role").value;
        var mudhohiFields = document.getElementById("mudhohi-fields");

        if (role === "mudhohi") {
          mudhohiFields.style.display = "block";
        } else {
          mudhohiFields.style.display = "none";
        }
      }
    </script>
  </body>
</html>
